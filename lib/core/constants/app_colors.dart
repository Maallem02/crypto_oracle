import 'package:flutter/material.dart';

class AppColors {
  // Private constructor - this class should never be instantiated
  AppColors._();

  // Background colors
  static const Color background     = Color(0xFF0A0E1A);
  static const Color cardBackground = Color(0xFF141926);
  static const Color surface        = Color(0xFF1E2535);

  // Brand colors
  static const Color primary        = Color(0xFF00D4FF);
  static const Color primaryDark    = Color(0xFF0099BB);

  // Semantic colors
  static const Color success        = Color(0xFF00E676);
  static const Color error          = Color(0xFFFF5252);
  static const Color warning        = Color(0xFFFFD740);

  // Text colors
  static const Color textPrimary    = Color(0xFFFFFFFF);
  static const Color textSecondary  = Color(0xFF8A94A6);
  static const Color textHint       = Color(0xFF4A5568);

  // Chart colors
  static const Color cardBorder     = Color(0xFF1E2D45);
  static const Color chartLine      = Color(0xFF00D4FF);
  static const Color chartFill      = Color(0x1A00D4FF);
  static const Color predictionLine = Color(0xFFFFD740);
}